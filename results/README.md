# Executed verification outputs

`validation.json` records the actual independent audit. PCA variance, loading and score tables contain all 17 components. LDA variance, score and coefficient tables contain all three discriminants. `soil-with-pear-counts.csv` retains all 96 joined trees. Quality subgroup and growth/retention tables retain the complete grouping breakdowns.

These are data-verification and descriptive multivariate outputs produced by `scripts/verify_data.py`. R/SAS model estimates remain available in the original PDFs; they were not re-fitted during repository preparation. Additional R/SAS outputs appear here only when those programs are run.
